import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'SiteGPT - Verified Resolution Agent',
  description: 'AI-powered helpdesk resolution for Shopify merchants. Resolve WISMO and Returns tickets with grounded data, policy checks, and citations.',
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  )
}
